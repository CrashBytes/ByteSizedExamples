# Copy to .env and fill in for the live example adapter (examples/demo.ts runs
# fully offline and needs none of these — they are only for real providers).
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the default model used by the Anthropic adapter.
ANTHROPIC_MODEL=claude-opus-4-8
