# Core AI Framework
# Bumped to LangChain 1.x — safe here because src/ only contains scaffolding
# __init__.py files; no langchain.* imports exist yet. Tutorial readers
# building from this baseline should use `langchain_core.*` import paths.
langchain==1.3.13
langchain-openai==1.3.5
langchain-community==0.4.2

# LLM Providers
openai==2.45.0

# Tool Dependencies
requests==2.34.2
beautifulsoup4==4.15.0
pandas==3.0.3  # held at 2.x for ecosystem compat
sqlalchemy==2.0.51
psycopg2-binary==2.9.12

# Vector Store and Embeddings
# chromadb removed 2026-07-04: CVE-2026-45829 (pre-auth RCE in the Chroma
# server API) affects every 1.x release and no patched version exists yet.
# Nothing in src/ imports chromadb — it was a forward declaration for the
# memory layer. Re-add (>= first patched release) when implementing it.
sentence-transformers==5.6.0

# Utilities
python-dotenv==1.2.2
pydantic==2.13.4
pyyaml==6.0.3
tenacity==9.1.4

# Monitoring and Logging
loguru==0.7.3
prometheus-client==0.25.0

# CLI Interface
typer==0.26.8
rich==15.0.0

# Development Tools
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
black==26.5.1
mypy==2.3.0
flake8==7.3.0
