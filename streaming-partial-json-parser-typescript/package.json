{
  "name": "streaming-partial-json-parser-typescript",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A tolerant streaming partial-JSON parser for LLM structured outputs: repair an incomplete token-by-token buffer and render fields progressively. Zero runtime dependencies. Companion code for the CrashBytes tutorial.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "tsx src/demo.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "nanoid": ">=3.3.18"
  }
}
