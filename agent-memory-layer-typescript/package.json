{
  "name": "agent-memory-layer-typescript",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A durable agent memory layer in TypeScript: recall, summarize, evict. Companion code for the CrashBytes tutorial.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "overrides": {
    "postcss": ">=8.5.18",
    "nanoid": ">=3.3.18"
  }
}
