{
  "name": "prompt-caching-cost-optimizer-typescript",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Structure Anthropic requests so the stable prefix caches and only the volatile suffix pays full price, then measure the realized savings. Companion code for the CrashBytes tutorial.",
  "license": "MIT",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^4.1.10",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
