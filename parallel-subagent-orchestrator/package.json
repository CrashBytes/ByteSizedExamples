{
  "name": "parallel-subagent-orchestrator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A parallel subagent orchestrator in TypeScript: fan-out, bounded concurrency, retries, schema-validated structured output, and barrier-free pipelines. Companion code for the CrashBytes tutorial.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
