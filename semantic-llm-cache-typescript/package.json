{
  "name": "semantic-llm-cache-typescript",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A semantic LLM cache in TypeScript: embed, nearest-neighbor lookup, threshold hit, TTL + LRU eviction. Zero runtime dependencies. Companion code for the CrashBytes tutorial.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
