{
  "name": "react-native-rag-tutorial",
  "version": "1.0.0",
  "private": true,
  "description": "Production RAG for React Native: a shared retrieval core that runs both on-device and behind a cloud retrieval service, with Claude Opus 4.8 answer synthesis.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "server",
    "app"
  ],
  "scripts": {
    "build": "npm run build -w @cb/rag-core && npm run build -w server",
    "typecheck": "npm run typecheck -w @cb/rag-core && npm run typecheck -w server",
    "test": "npm run test -w @cb/rag-core && npm run test -w server",
    "lint": "eslint packages server",
    "dev:server": "npm run dev -w server"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "xcode": {
      "uuid": "^11.1.1"
    }
  }
}
