# Copy to .env and fill in only if you wire the REAL provider adapters in
# src/providers/index.ts. The tests and `npm run demo` run fully offline with a
# fake in-memory adapter and need none of these keys.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...
