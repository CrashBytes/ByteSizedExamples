torch==2.12.0
torchvision==0.27.0
torchaudio==2.11.0

# Distributed Training
# horovod removed from the default install — project is dormant upstream
# (last release 0.28.1 in 2023), carries unpatched CRITICAL CVE
# GHSA-mrhh-3ggq-23p2, and its C++/cmake build fails against modern PyTorch.
# See SECURITY.md for install instructions if you need it for the tutorial.
mpi4py==4.1.2

# Experiment Tracking
mlflow==3.13.0
tensorboard==2.20.0

# Data Processing
numpy==2.4.6
pandas==2.3.3  # held at 2.x because mlflow 3.13 requires pandas<3
pillow==12.2.0
opencv-python==4.13.0.92

# Utilities
pyyaml==6.0.3
tqdm==4.68.2
psutil==7.2.2

# Testing
pytest==9.1.0
pytest-cov==7.1.0
pytest-asyncio==1.4.0
